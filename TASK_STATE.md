# Current task

- Request: Plus defaults to 5h; click quota area to alternate Weekly without another button; push public source.
- Changes: native display selection, per-account local persistence, matching percent/fill/reset/status track, documentation. Missing selected windows remain unavailable. Routing untouched.
- Runtime: existing installed build unchanged. Current-request compilation and UI acceptance require owner consent.
- Next: request a release build and installed pointer check; no model requests or account switches required.
